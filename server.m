fileName = 'mac_test.txt';

% Send file three times, as simple as that. AKA Modular code.
sendFile(fileName, tx, rx, radioConfigs);
sendFile(fileName, tx, rx, radioConfigs);
sendFile(fileName, tx, rx, radioConfigs);
